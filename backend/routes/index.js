// const blazemeter = require("./blazemeter");

// module.exports.routes = [
//   blazemeter, //
//   appdynamics, // Handles connection with AppDynamics api, processes results and sends to client
//   // query, // Connect to Oracle DB and run pre-defined queries, send results
//   metrics, // Endpoint to return the metrics between start-end time.
//   timescale, // Endpoint to return the oracle query results (performed by query agent) from timescale DB
//   validate, // for test running custom query / db settings
// ];
